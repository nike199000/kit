task "build" do
  exec("yarn build")
end

task "test" do
  exec("yarn test")
end

task "test:watch" do
  exec("yarn test:watch")
end

task "doc" do
  exec("yarn doc")
end

task "doc:open" do
  exec("yarn doc:open")
end
